import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
    dashboard: {
        // border:"1px solid red",
        marginTop: "2rem",
    },
    addImageBtn: {
        marginTop: "2rem"
    },
    cardContainer: {
        marginTop: "2rem"
    }
});